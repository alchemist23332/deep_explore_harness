# Java 21 Workspace

This workspace contains a minimal Maven project.

## Commands

```bash
mvn test
mvn package
java -cp target/classes com.example.App
```

Workspace files persist when the sandbox is stopped or recreated.
